name: Gradle Setup
description: Checkout, setup JDK 21, and configure Gradle

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: 21

    - uses: gradle/actions/setup-gradle@v6
